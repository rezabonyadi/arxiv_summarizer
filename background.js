// Start the server when the extension is installed or enabled
chrome.runtime.onInstalled.addListener(function() {
  startServer();
});

function startServer() {
  // Code to start the server goes here
}
